
using UnityEngine;
// Used to store static values which are used in globally
public class Values : MonoBehaviour
{
    public static bool MATCHISON = false; // used to start match
    public static bool Started = false; // used to tell everything is done before the match starts
    public static bool MusicOn = true;
}
